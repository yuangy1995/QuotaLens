# 2026-08-26 复审与本地测试交付

## 范围与结论

本次以用户提供的复审清单为范围，对照本地 `quota-monitor` 的 `6069983`，检查 GPT-5.5 xhigh 的首轮修复，再补齐遗漏并运行验证。原项目 `4785f3c` 中的重置卡修复保持不变。

修复代码已合入本地 `main`：首轮修复 `4be3818`，独立复审补修 `bd9d530`。未推送远端。打包源码为 `bd9d5301bb48531ea47653f24ddfa9bf4f36a8a6`。

复审清单中的实现项已完成；没有发现本轮范围内仍需阻止本地试用的问题。API 等价价值仍是本地估算，不是 OpenAI 实际账单。Developer ID、公证、Gatekeeper、安装器及安装信任链不纳入本轮评价。

## 修复清单

| 项目 | 最终处理 |
| --- | --- |
| 旧模型 Cache Write 导致整条事件为零 | 价格目录升级至 v5，旧模型 Cache Write 按同层级普通输入单价计价；保留分数 nano-USD 精度。 |
| Sol 促销切点 | 改为 `2026-08-21T19:34:10Z`，按事件时间而非扫描时间选择价格。该秒级边界沿用参考项目的公开公告时间，不宣称它是账单系统的精确切换时间。 |
| 历史模型价格边界 | 采用官方 API 可用日期、UTC 零点作为保守起点。修正 GPT-5-Codex 为 2025-09-23、GPT-5.1-Codex-Max 为 2025-12-04；公开列表价之前保持“缺少历史规则”。 |
| Fast 长上下文历史边界 | GPT-5.6 的 Fast／Priority 超过 272K 输入从 2026-08-05 起支持，之前不回填当前能力；标准／Flex 不受这项限制影响。 |
| Parser 升级清空历史 | 迁移保留正式索引；每个来源先写临时影子表，事件、会话、模型与日汇总计数及 Token 一致后原子发布。失败保留该来源的旧索引，下次重试。 |
| 原始日志缺失与早期仅聚合数据库 | 待重建但原件不可用的来源不会被扫描清理；显示 pending。仅有旧汇总而无事件账本时，暂缓重计价，不用空账本将旧历史清零。 |
| 重计价大事务与恢复 | 分批写持久化影子结果，记录代次、游标和进度，最后原子发布。恢复时校验目录摘要与事件身份，处理 rowid 被复用；汇总分页按 rowid 读取，日历计算在连接锁外完成。 |
| 日期分桶 | 导入、重计价及默认查询统一使用公历 Calendar 和显式 TimeZone。时区变化由已有事件账本重建全部日汇总，无须重读原始日志；发布失败时旧汇总与旧时区标记共同保留。 |
| 未计价原因丢失 | 保留聚合计价状态、六类未计价原因的事件数与 Token 数，贯穿会话、历史、Dashboard 和诊断导出；卡片展示 Token 加权覆盖率／原因，悬停可读完整说明。 |
| 非标准文件名日志漏扫 | 探测窗口扩大到 1 MiB／200 个非空行，兼容窗口截断多字节字符，持久化跳过原因；扫描失败不触发错误清理。 |
| 可见状态与重试 | 设置页展示 Parser 重建和重计价进度；部分扫描或原件缺失不会再被标记为完全成功。 |

历史边界通过 `openai-docs` 技能复核，采用 [OpenAI API 更新记录](https://developers.openai.com/api/docs/changelog) 中的 API 可用日期及 Fast 长上下文开放日期，不混用 Codex 产品发布日。

## 验证结果

在本机 macOS、arm64 上执行完整回归与临时百万事件压测：

```sh
QUOTALENS_RUN_PERF_BENCHMARK=1 swift test
```

结果：共 86 项，85 项通过，1 项真实历史烟测按设计跳过，0 失败。验证包含：

- 全目录模型／层级、272000／272001 边界、普通输入／Cache Read／Cache Write／输出，以及历史切点。
- 首次导入、无变化、追加、重写、截断、搬迁、删除、子会话 replay，以及三页面总量一致性。
- Parser 影子写入失败后旧索引保留、恢复原件后重试、早期聚合历史保留。
- 重计价中断后续跑、rowid 复用、时区发布失败回滚、原件缺失时基于账本重新分桶。
- 诊断原因分布、Token 加权覆盖、日期午夜／DST 边界。

百万事件压测使用合成数据：一个模型、一个会话、一日，共 1,000,000 条事件。金额与三层汇总一致。

| 阶段 | 累计耗时 |
| --- | ---: |
| 分批计价完成 | 10.411 秒 |
| 汇总完成、开始发布 | 14.004 秒 |
| 原子发布完成 | 17.162 秒 |
| 重计价总耗时 | 17.163 秒 |

最后发布阶段约 3.2 秒。阶段采样中 WAL 最大约 289.7 MiB，这不是连续采样的峰值保证。以上是 Debug 构建的合成基准，不代表真实数据量、会话分布或 UI 卡顿上限；未执行 500 万／1,000 万事件测试，也未做主线程及锁等待的专门性能采样。

没有导入或改写用户的真实 QuotaLens 数据库，也没有对完整私人 Codex 历史做逐笔账单核对。已完成测试的临时夹具自动清理；一次中断压测残留的临时数据库已移到废纸篓，可恢复。验证后按用户要求删除了独立的临时压测文件（Git 中可恢复）；既有回归文件的必要修正保留。因此最终源码的常规 `swift test` 不再包含上述 opt-in 压测。

清理后又在主项目目录执行 `swift test`：85 项中 84 项通过、1 项真实历史烟测跳过，0 失败。

## 本地包与试用建议

交付版本为 `1.0.13`，构建号 `41`，Universal（arm64＋x86_64），Release 构建。本地包输出至 `dist/local-review-20260826/`；原有 `dist` 中的旧包保持不变。

两个架构分别编译成功；已核验应用和 Sparkle 的双架构、Info.plist 版本、ZIP 数据完整性及 DMG 校验和。包内未包含测试数据库、JSONL 夹具或测试套件。未启动新应用操作真实账号，也未进行 Intel 真机交互测试。

| 文件 | 大小（字节） | SHA-256 |
| --- | ---: | --- |
| `QuotaLens-v1.0.13-macOS-universal.dmg` | 8,168,881 | `d3c08b1085155c0dc3434bfa49352fb74d8cda32e8614f1d092f9dc0e50a22cf` |
| `QuotaLens-v1.0.13-macOS-universal.zip` | 7,104,394 | `d05c3f93fda9e72deaea50112aaeb97797cc557b5e14768a87f3939cc8aa9c85` |

先退出旧版，再打开新包中的应用。建议重点测试：

1. 设置页扫描状态、完整重建与诊断导出。
2. 含旧模型 Cache Write 的会话金额；同一时间范围下 Sessions、History 与 Dashboard 的 Token／金额是否一致。
3. 未计价事件是否有明确原因，而不是看起来像免费调用。
4. 旧日志已移动或删除时，待重建的历史是否仍保留并提示重试。

如果旧版本曾经清空数据库且原始日志也已经删除，本次修复不能凭空恢复已丢失的数据；它保证后续升级和失败重建不再主动丢掉现有旧索引。
