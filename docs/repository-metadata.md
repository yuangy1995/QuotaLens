# Repository metadata

The proposed GitHub About description for the native Windows integration is:

> Native macOS and Windows (preview) dashboards for Codex, Claude and Antigravity quotas, local usage analytics, forecasts and desktop overlays.

This text file is not the GitHub repository settings. A source commit does not update the About description automatically. An authorized maintainer can apply only this field with GitHub CLI:

```bash
gh repo edit yuangy1995/QuotaLens --description "Native macOS and Windows (preview) dashboards for Codex, Claude and Antigravity quotas, local usage analytics, forecasts and desktop overlays."
```

Do not place an administration token in code, a workflow, screenshots or documentation to automate this change. Verify the repository settings separately after running the command.
